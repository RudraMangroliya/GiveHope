# Server Port configuration
PORT=5000

# MongoDB connection connection string (replace with your MongoDB Atlas or production URI)
MONGO_URI=mongodb://127.0.0.1:27017/donation_app

# Secret key used to encrypt and sign JWT authorization tokens
JWT_SECRET=your_jwt_secret_key_here
